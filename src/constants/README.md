# src/constants/

This directory intentionally left empty.

**Removed (2026-05-01):** `colors.ts` was dead code. The project uses `src/theme/colors.ts` instead. The test file `src/__tests__/constants/colors.test.ts` was also removed as it tested this dead code.